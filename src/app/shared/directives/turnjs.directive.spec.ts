import { TurnjsDirective } from './turnjs.directive';

describe('TurnjsDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnjsDirective();
    expect(directive).toBeTruthy();
  });
});
